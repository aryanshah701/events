<h1>Edit Event</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.event_path(@conn, :update, @event)) %>

<span><%= link "Back to Event", to: Routes.event_path(@conn, :show, @event) %></span>
